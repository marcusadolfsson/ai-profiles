[package]
name = "ai-profiles"
version = "1.3.1"
description = "Run multiple Claude accounts on one Mac, each with its own isolated Desktop launcher and CLI wrapper."
authors = ["Bartek Czyż <bartek@czyz.it>"]
repository = "https://github.com/bartekczyz/ai-profiles"
license = "MIT"
homepage = "https://czyz.it?utm_source=ai-profiles"
edition = "2021"
# The workspace below adds a second binary (`profile-shim`), which makes a bare
# `cargo run` (and `tauri dev`) ambiguous without this.
default-run = "ai-profiles"

# `profile-shim` is the executable of every generated wrapper bundle. build.rs
# compiles it for the target being built and the app embeds the bytes; being a
# default member keeps its tests, clippy and rustfmt inside the usual commands.
# `ai-profiles-core` is what the app shares with ai-profiles-server, the daemon
# a Linux machine runs so ai-profiles can manage its Claude sessions (`server`).
[workspace]
members = ["shim", "core", "server"]
default-members = [".", "shim", "core", "server"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "ai_profiles_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
async-trait = "0.1"
dirs = "6"
icns = "0.4.0"
plist = "1.9.0"
trash = "5.2.6"
tauri-plugin-clipboard-manager = "2"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json", "gzip", "brotli", "deflate"] }
sha2 = "0.10"
tokio = { version = "1", features = ["rt", "macros", "process", "time"] }
portable-pty = "0.9.0"
profile-shim = { path = "shim" }
ai-profiles-core = { path = "core", features = ["tls"] }

[dev-dependencies]
tempfile = "3.27.0"
# The remote-host client is tested against the real server, in-process.
ai-profiles-server = { path = "server" }
tokio = { version = "1", features = ["net", "sync", "rt-multi-thread", "io-util"] }
# A host that takes a request and never answers it.
tokio-rustls = { version = "0.26", default-features = false, features = ["ring"] }

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-process = "2"
tauri-plugin-updater = "2"
tauri-plugin-window-state = "2"

# Focus / reopen a specific already-running Claude instance by PID
# (single-instance launch behaviour). Already present transitively via tauri.
[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-app-kit = { version = "0.3", features = [
    "NSBitmapImageRep",
    "NSGraphics",
    "NSGraphicsContext",
    "NSImage",
    "NSImageRep",
    "NSRunningApplication",
    "NSWorkspace",
] }
objc2-foundation = { version = "0.3", features = [
    "NSAppleEventDescriptor",
    "objc2-core-services",
    "libc",
    "NSDate",
    "NSError",
] }

# Profile for the `profile-shim` binary build.rs embeds. One copy is written
# into every wrapper bundle, so it is built for size.
[profile.shim]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

